/*#ifndef ENTITY_CONTAINER_H
#define ENTITY_CONTAINER_H

#include "Entity.h"
#include <map>
#include <unordered_map>
*/
/*
 * A small container that sorts entities
 * based on their layers
 * Can add/remove entities with ease
 *//*
class EntityContainer {
public:
    EntityContainer(); 

    void add(std::string ID, Entity* entity); // Appends entity based on ID type

    int size();



private:
    std::map<std::string, Entity*> entityList; // Map of entities
    std::unordered_map<std::string, int> entityIDCountList; // Counts entities by ID
};

#endif // ENTITY_CONTAINER_H*/